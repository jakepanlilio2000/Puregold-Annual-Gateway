<Window x:Class="LocatorAutoPrint.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:LocatorAutoPrint.ViewModels"
        xmlns:views="clr-namespace:LocatorAutoPrint.Views.UserControls"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        Title="Locator Auto-Print &amp; Edit" 
        Width="565" Height="565" 
        ResizeMode="NoResize" 
        WindowStartupLocation="CenterScreen" 
        Background="{StaticResource Brush.LightGray}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <!-- Header -->
            <RowDefinition Height="46"/>
            <!-- Top Navigation -->
            <RowDefinition Height="*"/>
            <!-- Content -->
            <RowDefinition Height="28"/>
            <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- HEADER: Dark Blue Structure -->
        <Border Grid.Row="0" Background="{StaticResource Brush.DarkBlue}">
            <Grid Margin="16,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <fa:IconBlock Icon="Barcode" Foreground="{StaticResource Brush.PrimaryYellow}" FontSize="14" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding HeaderHostDisplay, Mode=OneWay}" Foreground="{StaticResource Brush.White}" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Foreground="{StaticResource Brush.White}" FontSize="11" VerticalAlignment="Center">
                        <Run Text="Connections: "/>
                    </TextBlock>
                    <TextBlock Text="{Binding HeaderStatus.UnclosedLocators, Mode=OneWay}" Foreground="{StaticResource Brush.PrimaryYellow}" FontWeight="Bold" FontSize="11" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- NAVIGATION: Horizontal Pill Tabs -->
        <Border Grid.Row="1" Background="{StaticResource Brush.White}" BorderBrush="{StaticResource Brush.NeutralGray}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="12,0" VerticalAlignment="Center">
                <Button Command="{Binding ShowPrintCommand}" Style="{StaticResource NavPillButton}">
                    <StackPanel Orientation="Horizontal">
                        <fa:IconBlock Icon="Print" FontSize="12" Margin="0,0,6,0"/>
                        <TextBlock Text="Print Locator"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding ShowEditCommand}" Style="{StaticResource NavPillButton}">
                    <StackPanel Orientation="Horizontal">
                        <fa:IconBlock Icon="FilePen" FontSize="12" Margin="0,0,6,0"/>
                        <TextBlock Text="Edit Count Sheet"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding ShowReportsCommand}" Style="{StaticResource NavPillButton}">
                    <StackPanel Orientation="Horizontal">
                        <fa:IconBlock Icon="ChartColumn" FontSize="12" Margin="0,0,6,0"/>
                        <TextBlock Text="Reports"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding ShowUsersCommand}" Style="{StaticResource NavPillButton}">
                    <StackPanel Orientation="Horizontal">
                        <fa:IconBlock Icon="Users" FontSize="12" Margin="0,0,6,0"/>
                        <TextBlock Text="Users"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- CONTENT AREA -->
        <ContentControl Grid.Row="2" Content="{Binding CurrentViewModel}" Margin="0">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type local:LocatorPrintViewModel}">
                    <views:LocatorPrintView />
                </DataTemplate>
                <DataTemplate DataType="{x:Type local:EditCountSheetViewModel}">
                    <views:EditCountSheetView />
                </DataTemplate>
                <DataTemplate DataType="{x:Type local:ReportsViewModel}">
                    <views:ReportsView />
                </DataTemplate>
                <DataTemplate DataType="{x:Type local:UsersViewModel}">
                    <views:UsersView />
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>

        <!-- FOOTER: Status & Time -->
        <Border Grid.Row="3" Background="{StaticResource Brush.White}" BorderBrush="{StaticResource Brush.NeutralGray}" BorderThickness="0,1,0,0">
            <Grid Margin="16,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <fa:IconBlock Icon="Database" FontSize="10" Margin="0,0,6,0" VerticalAlignment="Center">
                        <fa:IconBlock.Style>
                            <Style TargetType="fa:IconBlock">
                                <Setter Property="Foreground" Value="#2ECC71"/>
                                <!-- Green -->
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DbStatus}" Value="SQL Server Disconnected">
                                        <Setter Property="Foreground" Value="#E74C3C"/>
                                        <!-- Red -->
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </fa:IconBlock.Style>
                    </fa:IconBlock>
                    <TextBlock Text="{Binding FooterDbDisplay, Mode=OneWay}" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#2ECC71"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DbStatus}" Value="SQL Server Disconnected">
                                        <Setter Property="Foreground" Value="#E74C3C"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <fa:IconBlock Icon="Clock" Foreground="{StaticResource Brush.NeutralGray}" FontSize="10" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentTime}" Foreground="#7F8C8D" FontSize="11" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>