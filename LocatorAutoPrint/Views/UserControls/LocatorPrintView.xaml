<UserControl x:Class="LocatorAutoPrint.Views.UserControls.LocatorPrintView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             HorizontalAlignment="Stretch" 
             VerticalAlignment="Stretch" 
             Background="{StaticResource Brush.LightGray}">

    <Grid Margin="16,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Top Actions -->
            <RowDefinition Height="Auto"/>
            <!-- Input Section -->
            <RowDefinition Height="Auto"/>
            <!-- Stats Header -->
            <RowDefinition Height="160"/>
            <!-- Stats Cards -->
            <RowDefinition Height="Auto"/>
            <!-- Primary Actions -->
            <RowDefinition Height="Auto"/>
            <!-- Secondary Actions -->
        </Grid.RowDefinitions>

        <!-- TOP RIGHT UTILITY BUTTONS -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
            <Button Command="{Binding ShowGuideCommand}" Style="{StaticResource SecondaryActionButton}" Padding="8,4" Margin="0,0,6,0" ToolTip="User Guide">
                <fa:IconBlock Icon="CircleQuestion" FontSize="12"/>
            </Button>
            <Button Command="{Binding ShowHowToCommand}" Style="{StaticResource SecondaryActionButton}" Padding="8,4" Margin="0,0,6,0" ToolTip="How to Use">
                <fa:IconBlock Icon="Lightbulb" FontSize="12"/>
            </Button>
            <Button Command="{Binding ShowAppInfoCommand}" Style="{StaticResource SecondaryActionButton}" Padding="8,4" ToolTip="App Info">
                <fa:IconBlock Icon="CircleInfo" FontSize="12"/>
            </Button>
        </StackPanel>

        <!-- INPUT SECTION -->
        <Border Grid.Row="1" Background="{StaticResource Brush.White}" BorderBrush="{StaticResource Brush.NeutralGray}" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,12,0">
                    <TextBlock Text="Enter Locator Number(s)" Style="{StaticResource Text.Header}" FontSize="12"/>
                </StackPanel>

                <TextBox Grid.Column="1" 
                         Text="{Binding LocatorInput, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource ModernTextBox}" 
                         AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto"
                         Height="48"
                         VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- STATS HEADER -->
        <TextBlock Grid.Row="2" Text="Live Progress Report" Style="{StaticResource Text.Header}" Margin="0,0,0,8"/>

        <!-- STATS CARDS (2x2 Grid) -->
        <!-- STATS CARDS (2x2 Grid) -->
        <Grid Grid.Row="3" Margin="0,0,0,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- OVERALL -->
            <Border Grid.Row="0" Grid.Column="0" Background="{StaticResource Brush.White}" BorderBrush="{StaticResource Brush.NeutralGray}" BorderThickness="1" CornerRadius="6" Margin="0,0,6,6" Padding="12">
                <Grid>
                    <TextBlock Text="OVERALL" Style="{StaticResource Text.Header}" FontSize="11" VerticalAlignment="Top"/>
                    <TextBlock Text="{Binding Stats.Overall.Pct, StringFormat={}{0:N2}%}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource Brush.PrimaryBlue}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                    <TextBlock VerticalAlignment="Bottom" FontSize="9" Foreground="#7F8C8D" TextWrapping="NoWrap">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="Done: {0:0} | Cxl: {1:0} | Tot: {2:0} | Rem: {3:0}">
                                <Binding Path="Stats.Overall.Comp"/>
                                <Binding Path="Stats.Overall.Cancel"/>
                                <Binding Path="Stats.Overall.Total"/>
                                <Binding Path="Stats.Overall.Rem"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </Grid>
            </Border>

            <!-- SELLING -->
            <Border Grid.Row="0" Grid.Column="1" Background="{StaticResource Brush.White}" BorderBrush="{StaticResource Brush.NeutralGray}" BorderThickness="1" CornerRadius="6" Margin="6,0,0,6" Padding="12">
                <Grid>
                    <TextBlock Text="SELLING" Style="{StaticResource Text.Header}" FontSize="11" VerticalAlignment="Top"/>
                    <TextBlock Text="{Binding Stats.Selling.Pct, StringFormat={}{0:N2}%}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource Brush.PrimaryBlue}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                    <TextBlock VerticalAlignment="Bottom" FontSize="9" Foreground="#7F8C8D" TextWrapping="NoWrap">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="Done: {0:0} | Cxl: {1:0} | Tot: {2:0} | Rem: {3:0}">
                                <Binding Path="Stats.Selling.Comp"/>
                                <Binding Path="Stats.Selling.Cancel"/>
                                <Binding Path="Stats.Selling.Total"/>
                                <Binding Path="Stats.Selling.Rem"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </Grid>
            </Border>

            <!-- WAREHOUSE -->
            <Border Grid.Row="1" Grid.Column="0" Background="{StaticResource Brush.White}" BorderBrush="{StaticResource Brush.NeutralGray}" BorderThickness="1" CornerRadius="6" Margin="0,0,6,0" Padding="12">
                <Grid>
                    <TextBlock Text="WAREHOUSE" Style="{StaticResource Text.Header}" FontSize="11" VerticalAlignment="Top"/>
                    <TextBlock Text="{Binding Stats.Warehouse.Pct, StringFormat={}{0:N2}%}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource Brush.PrimaryBlue}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                    <TextBlock VerticalAlignment="Bottom" FontSize="9" Foreground="#7F8C8D" TextWrapping="NoWrap">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="Done: {0:0} | Cxl: {1:0} | Tot: {2:0} | Rem: {3:0}">
                                <Binding Path="Stats.Warehouse.Comp"/>
                                <Binding Path="Stats.Warehouse.Cancel"/>
                                <Binding Path="Stats.Warehouse.Total"/>
                                <Binding Path="Stats.Warehouse.Rem"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </Grid>
            </Border>

            <!-- BUFFER -->
            <Border Grid.Row="1" Grid.Column="1" Background="{StaticResource Brush.White}" BorderBrush="{StaticResource Brush.NeutralGray}" BorderThickness="1" CornerRadius="6" Margin="6,0,0,0" Padding="12">
                <Grid>
                    <TextBlock Text="BUFFER" Style="{StaticResource Text.Header}" FontSize="11" VerticalAlignment="Top"/>
                    <TextBlock Text="{Binding Stats.Buffer.Pct, StringFormat={}{0:N2}%}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource Brush.PrimaryBlue}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                    <TextBlock VerticalAlignment="Bottom" FontSize="9" Foreground="#7F8C8D" TextWrapping="NoWrap">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="Done: {0:0} | Cxl: {1:0} | Tot: {2:0} | Rem: {3:0}">
                                <Binding Path="Stats.Buffer.Comp"/>
                                <Binding Path="Stats.Buffer.Cancel"/>
                                <Binding Path="Stats.Buffer.Total"/>
                                <Binding Path="Stats.Buffer.Rem"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </Grid>
            </Border>
        </Grid>

        <!-- PRIMARY ACTIONS -->
        <Grid Grid.Row="4" Margin="0,0,0,8">
            <Button Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryActionButton}" HorizontalAlignment="Left" Width="120">
                <StackPanel Orientation="Horizontal">
                    <fa:IconBlock Icon="ArrowsRotate" FontSize="11" Margin="0,0,6,0"/>
                    <TextBlock Text="Refresh Data"/>
                </StackPanel>
            </Button>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Command="{Binding CloseCommand}" Style="{StaticResource SecondaryActionButton}" Width="90" Margin="0,0,8,0" IsCancel="True">
                    <StackPanel Orientation="Horizontal">
                        <fa:IconBlock Icon="Xmark" FontSize="11" Margin="0,0,6,0"/>
                        <TextBlock Text="Cancel"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding PrintCommand}" Style="{StaticResource PrimaryActionButton}" Width="100" IsDefault="True">
                    <StackPanel Orientation="Horizontal">
                        <fa:IconBlock Icon="Check" FontSize="11" Margin="0,0,6,0"/>
                        <TextBlock Text="OK / Print"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- SECONDARY ACTIONS -->
        <Border Grid.Row="5" Background="{StaticResource Brush.White}" BorderBrush="{StaticResource Brush.NeutralGray}" BorderThickness="1" CornerRadius="4" Padding="6">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Command="{Binding OpenCountsheetListCommand}" Style="{StaticResource SecondaryActionButton}" Padding="10,4" Margin="0,0,6,0" FontSize="11">
                    <StackPanel Orientation="Horizontal">
                        <fa:IconBlock Icon="ListCheck" FontSize="10" Margin="0,0,6,0"/>
                        <TextBlock Text="Countsheet List"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding OpenPrelocMaintenanceCommand}" Style="{StaticResource SecondaryActionButton}" Padding="10,4" Margin="0,0,6,0" FontSize="11">
                    <StackPanel Orientation="Horizontal">
                        <fa:IconBlock Icon="Gears" FontSize="10" Margin="0,0,6,0"/>
                        <TextBlock Text="PRELOC Maintenance"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding RestoreLocatorCommand}" Style="{StaticResource SecondaryActionButton}" Padding="10,4" FontSize="11">
                    <StackPanel Orientation="Horizontal">
                        <fa:IconBlock Icon="RotateLeft" FontSize="10" Margin="0,0,6,0"/>
                        <TextBlock Text="Restore Locator"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>